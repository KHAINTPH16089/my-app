export type productType = {
    _id?: string | undefined,
    name: string,
    price: number,
    category: string
}
export type userType = {
    userName: string,
    email: string,
    password: string
}